import { Component } from '@angular/core';
import { Topbar } from '../topbar/topbar';

@Component({
  selector: 'app-quests',
  imports: [Topbar],
  templateUrl: './quests.html',
  styleUrl: './quests.css',
})
export class Quests {

}
